This is a dotfiles repository managed by chezmoi.

- Whenever the user tell you to modify configuration, always edit files in the source directory (this repo), not the target directory (home directory)
- The `home/` directory contains all dotfiles that will be installed to the user's home directory
- Use the Conventional Commits specification
